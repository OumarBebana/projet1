[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 manage.py migrate --noinput && python3 manage.py collectstatic --noinput && gunicorn govnews.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
